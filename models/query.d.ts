export interface ScryfallQuery {
    color?: string, // color
    commander?: string, //
    type?: string,
    game?: string, // attention (game:paper)
    mana?: string, // {R}
    pow?: number, //
    rarity?: string, // u
    is?: string, 
    artist?: string,
    set?: string,
}