export interface ScryfallQuery {
    color?: string,
    commander?: string, // same as color
    type?: string,
    game?: string, // attention (game:paper)
    mana?: string, // {R}
    pow?: number, //
    rarity?: string, // u
    is?: string, 
    artist?: string,
    set?: string,
}